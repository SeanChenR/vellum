/**
 * ApiKeysPage — thin re-export wrapper. Canonical impl in `./ApiKeysTab`
 * (NO pricing table — that moved to PricingTab.tsx).
 * `/account/api-keys` is handled by `ApiKeysRouteRedirect`.
 */

export { ApiKeysTab as ApiKeysPage } from "./ApiKeysTab";
