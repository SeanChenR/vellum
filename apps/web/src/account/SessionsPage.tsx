/**
 * SessionsPage — thin re-export wrapper. Canonical impl in `./SessionsTab`.
 * `/account/sessions` is handled by `SessionsRouteRedirect`.
 */

export { SessionsTab as SessionsPage } from "./SessionsTab";
