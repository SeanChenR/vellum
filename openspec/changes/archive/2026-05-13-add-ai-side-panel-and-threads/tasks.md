## 1. DB schema + migration（對應 design「Multi-thread per (user, canvas) — schema 拿掉 unique constraint」「Cascade thread on canvas delete」）

- [x] 1.1 在 apps/api/src/db/schema.ts 加 `aiThreads` drizzle pgTable（id pk, user_id FK→users cascade, canvas_id FK→canvases cascade, title text not null default '', created_at, updated_at）+ 複合 index `(user_id, canvas_id, updated_at desc)`，**不**加 `(user_id, canvas_id)` unique；以 schema.test.ts 對應斷言驗證 column / FK / index 形狀（覆蓋 spec「Threads are persisted per user and canvas」）。
- [x] 1.2 [P] 在 apps/api/src/db/schema.ts 加 `aiMessages` drizzle pgTable（id pk, thread_id FK→aiThreads cascade, role check in('user','assistant','tool'), content jsonb, tool_name text null, tool_call_id text null, token_usage jsonb null, provider text null, model text null, run_id text null, created_at）+ 複合 index `(thread_id, created_at)`；以 schema.test.ts 對應斷言驗證 column / check / FK / index（覆蓋 spec「Thread messages are persisted in chronological order」）。
- [x] 1.3 透過 `bunx drizzle-kit generate` 產出 apps/api/drizzle/0006_ai_threads.sql；review 產出的 SQL 確認 cascade FK 正確、無 unique constraint on (user_id, canvas_id)；以 `bunx drizzle-kit migrate` apply 後在 dev DB 用 psql 確認兩張表 + 兩個 index 存在（覆蓋 design 決策「Multi-thread per (user, canvas) — schema 拿掉 unique constraint」「Cascade thread on canvas delete」）。

## 2. Thread Repo + CRUD endpoints（TDD 縱切，對應 design「Run endpoint body shape — `{threadId, userMessage}`，server 從 thread 讀歷史」）

- [x] 2.1 在 apps/api/src/agent/threads/repo.test.ts 撰寫 RED 測試：`loadMessages(threadId)` 依 created_at asc 排序回傳；`appendMessage(threadId, message)` 寫入並更新 thread.updated_at；`setUsageOnLastAssistant(threadId, runId, usage)` 只更新最近一筆 role=assistant 行；對應 spec「Thread messages are persisted in chronological order」。
- [x] 2.2 在 apps/api/src/agent/threads/repo.ts 實作上述三個函式 + `createThread(userId, canvasId, title)`、`listThreads(userId, canvasId)`、`getThread(threadId)`、`clearMessages(threadId)`、`deleteThread(threadId)`，使 2.1 測試 GREEN（覆蓋 spec「Thread CRUD endpoints for the active user」、「Thread token usage aggregation endpoint」）。
- [x] 2.3 在 apps/api/src/agent/threads/handlers.test.ts 撰寫 RED 測試：`GET /api/agent/threads/canvas/:canvasId` 對未存在 thread 的 user 自動 lazy-create 一條空 thread 並回 activeThreadId 指向它；對應 spec「Thread CRUD endpoints for the active user」的 lazy-create scenario。
- [x] 2.4 撰寫 RED 測試：`POST /api/agent/threads/canvas/:canvasId` 建立空 thread 回 201；`POST /api/agent/threads/:threadId/clear` 清空 messages 但保留 thread row 回 204；`DELETE /api/agent/threads/:threadId` 連 thread row + messages cascade 回 204；對應 spec「Thread CRUD endpoints for the active user」「Threads are persisted per user and canvas」cascade scenario。
- [x] 2.5 撰寫 RED 測試：cross-user 存取（U2 對 U1 的 thread 任一 endpoint 操作）一律回 403 + `agent.error.permissionDenied`，且不區分「thread 不存在」與「thread 存在但不屬於你」（不洩漏存在性）；對應 spec「Thread CRUD endpoints for the active user」的 cross-user isolation scenario。
- [x] 2.6 撰寫 RED 測試：`GET /api/agent/threads/:threadId` 回傳 `{thread, messages, usage}`；usage 為 sum across non-null token_usage rows；空 thread 為 `{input: 0, output: 0}`；對應 spec「Thread token usage aggregation endpoint」。
- [x] 2.7 在 apps/api/src/agent/threads/handlers.ts 實作上述四個 handler，使 2.3–2.6 GREEN；handler 注入 `ThreadRepo` deps + session（從既有 auth middleware 取 userId）。
- [x] 2.8 在 apps/api/src/index.ts 掛載 `/api/agent/threads/*` 路由 + 套用既有 session middleware；以 integration test 驗證一條完整路徑（authenticated GET → 自動 lazy-create thread）能跑通。

## 3. Background title generation（對應 design「Background title generation — 該 provider 的 economy 檔，fire-and-forget」「Background title generation 不計入 AGENT_RUN_RULE，但計入 BYOK 額度」「Title 不可由使用者手動編輯（M14 範圍內）」）

- [x] 3.1 在 apps/api/src/agent/title-gen.test.ts 撰寫 RED 測試：`buildFallbackTitle(firstUserMessage)` 切 30 字 word boundary、超出加 `…`、空字串回 `""`；對應 spec「Background title generation after first run」的 fallback 規則。
- [x] 3.2 撰寫 RED 測試：`PROVIDER_TITLE_MODELS` 常數表正好三個 entry — `openai → "gpt-4o-mini"`、`anthropic → "claude-haiku-4-5"`、`google → "gemini-2.5-flash-lite"`；新加 provider 必須補對應 entry 否則 typecheck fail；對應 spec「First-run completion triggers background title generation」。
- [x] 3.3 撰寫 RED 測試：`generateTitleInBackground(deps, threadId, provider, firstUserMessage)` 用 fake provider 回 `"Greeting Markdown Shape"` 後 thread.title UPDATE 為該值；fake throw 後 thread.title 維持 fallback、Pino warn 寫入 `event=ai_title_gen_failed`；對應 spec「Background title generation after first run」的成功與失敗 scenario。
- [x] 3.4 撰寫 RED 測試：`generateTitleInBackground` 跳過 `AGENT_RUN_RULE` 檢查（不呼叫 RateLimiter.limit），但**仍**透過 BYOK Vault 拿 user key 對 provider 發 request（覆蓋 design「Background title generation 不計入 AGENT_RUN_RULE，但計入 BYOK 額度」與 spec「First-run completion triggers background title generation」）。
- [x] 3.5 在 apps/api/src/agent/title-gen.ts 實作 `buildFallbackTitle`、`PROVIDER_TITLE_MODELS`、`generateTitleInBackground`；reuse 既有 BYOK Vault decrypt + Vercel AI SDK `streamText`；trim 結果（去頭尾空白與引號）後 UPDATE；**不**暴露 PATCH endpoint 讓使用者手動改 title（對應 design「Title 不可由使用者手動編輯（M14 範圍內）」）；確保 3.1–3.4 GREEN（覆蓋 spec「Background title generation after first run」「First-run completion triggers background title generation」）。

## 4. Streaming channel: run endpoint body shape + done.usage 欄位（TDD，對應 design「Run endpoint body shape」「`done` SSE event payload 加 `usage` 欄位」）

- [x] 4.1 在 packages/shared/src/agent-events.test.ts 擴充 RED 測試：`AgentEventDone` zod schema 接受 `{type: "done", runId, usage: {input, output, provider, model}}` 與 `usage: null` 兩種；舊形狀 `{type: "done", runId}` 不含 usage 欄位 parse 失敗；對應 spec「SSE event types are a closed discriminated union」。
- [x] 4.2 在 packages/shared/src/agent-events.ts 修 `AgentEventDone` discriminated union variant 加 `usage` 欄位（含 zod schema）；export 對應 TypeScript 型別；確保 4.1 GREEN（覆蓋 spec「SSE event types are a closed discriminated union」MODIFIED 內容）。
- [x] 4.3 在 apps/api/src/agent/sse-endpoint.test.ts 修既有 run handler RED 測試：body 從 `{messages, sessionId}` 改成 `{threadId, userMessage}`；body 含 `messages` field → 400 + invalidRequest；body 缺 `threadId` → 400；對應 spec「Run endpoint accepts POST with model selection」MODIFIED 行為。
- [x] 4.4 撰寫 RED 測試：thread 不屬於 session.userId → 400 + `invalidRequest`（不洩漏存在性，回應與不存在的 threadId 一致）；對應 spec「Run endpoint accepts POST with model selection」thread ownership scenario。
- [x] 4.5 撰寫 RED 測試：成功 run 流程中，server 在 first provider call 之前先 append `userMessage` 到 thread；run 結束時 SSE done event payload 含 `usage: {input, output, provider, model}`（fake provider 回對應 usage object）；provider 沒回 usage 時 SSE done 帶 `usage: null` + Pino warn `event=ai_provider_missing_usage`；對應 spec「Run endpoint accepts POST with model selection」「SSE event types are a closed discriminated union」provider missing usage scenario。
- [x] 4.6 在 apps/api/src/agent/sse-endpoint.ts 修改 `runBodySchema` 為 `{runId?, provider, model, threadId, userMessage}`；run handler 改成從 thread 讀歷史 → append userMessage → 把 `[...history, newUser]` 餵給 runtime；確保 4.3–4.5 GREEN。

## 5. Agent runtime: thread integration + title-gen trigger（TDD，對應 design「Run endpoint body shape」與 spec「Runtime loads conversation history from thread storage」「Runtime persists every emitted message to the thread」「First-run completion triggers background title generation」）

- [x] 5.1 在 apps/api/src/agent/runtime.test.ts 擴充 RED 測試組 H — Thread integration：`runAgent` deps 加 `threadRepo`；run 開頭呼叫 `threadRepo.appendMessage(threadId, userMessage)` 一次；run 過程每個 assistant text / tool_call / tool_result 都呼一次 `appendMessage`；對應 spec「Runtime loads conversation history from thread storage」「Runtime persists every emitted message to the thread」。
- [x] 5.2 撰寫 RED 測試組 I — Cancel preserves persisted prefix：fake 流：1 user + 2 assistant text + 1 tool_call + 1 tool_result 後 cancel；驗 `appendMessage` 共 5 次（不含後續）、無 delete / mutation 已寫的 row；對應 spec「Runtime persists every emitted message to the thread」cancel preserves scenario。
- [x] 5.3 撰寫 RED 測試組 J — Done usage written to last assistant：fake provider 回 `usage: {input: 1000, output: 500}`；確認 `setUsageOnLastAssistant(threadId, runId, usage)` 被呼叫一次且只更新 R1 emit 的最後一筆 assistant 行；對應 spec「Runtime persists every emitted message to the thread」done event scenario。
- [x] 5.4 撰寫 RED 測試組 K — First-run title trigger：fake `titleGen.generateTitleInBackground` spy；當 thread.title 等於 fallback 時 done event 後 5 秒內 spy 被呼叫一次；當 thread.title 已非 fallback 時 spy 不被呼叫；對應 spec「First-run completion triggers background title generation」雙 scenario。
- [x] 5.5 在 apps/api/src/agent/runtime.ts 修 `RunRequest` 介面（用 thread loaded messages 而非 client supplied）+ 加 `threadRepo` 與 `titleGen` 兩個 deps；實作上述行為；確保 5.1–5.4 GREEN。
- [x] 5.6 在 apps/api/src/agent/wiring.ts 把 `threadRepo` 與 `titleGen` 注入；確認既有 M13 lifecycle / cancel / timeout / cap / permission / BYOK / retry / error 8 個 spec scenario 仍 GREEN（regression check）。

## 6. Provider routing 改 explicit map（design「Provider routing 從 `model.startsWith()` 改成 explicit map」）

- [x] 6.1 在 apps/api/src/agent/wiring.test.ts（若無則新增）撰寫 RED 測試：`PROVIDER_MODELS: Record<ProviderName, ReadonlySet<ModelId>>` 三個 entry；`buildModel(provider, modelId)` 對 unknown modelId throw 含 `agent.error.invalidRequest` 的錯；新加 model 必須同時出現在 BYOK pricing 表與 PROVIDER_MODELS（unit test 對齊兩表 cardinality 一致）。
- [x] 6.2 在 apps/api/src/agent/wiring.ts 把 `model.startsWith(...)` chain 替換成 `PROVIDER_MODELS.<provider>.has(modelId)` 查表；unknown → throw；確保 6.1 GREEN 且 5.6 regression 仍 GREEN（覆蓋 design「Provider routing 從 `model.startsWith()` 改成 explicit map」）。

## 7. Tool registry description quality（TDD，對應 design「Tool description per-shape-type quality — updateShape / connectShapes / groupShapes」與 spec「Tool registry enumerates the full agent tool surface」）

- [x] 7.1 在 apps/api/src/sync/tool-registry.test.ts 擴充 RED 測試：`toolRegistry.updateShape.description` 字串包含 `markdown` / `code` / `callout` / `link-card` 四個 substring + 至少一個對應 patch key（content / source / language / variant / body / url / metadata）；對應 spec「Tool registry enumerates the full agent tool surface」updateShape scenario。
- [x] 7.2 撰寫 RED 測試：`toolRegistry.connectShapes.description` 提到 start / end shape id 與 arrow style props（color / dash / bend）至少一個；對應 spec connectShapes scenario。
- [x] 7.3 撰寫 RED 測試：`toolRegistry.groupShapes.description` 提到 children retain absolute positions + group 自身無 editable props；對應 spec groupShapes scenario。
- [x] 7.4 在 apps/api/src/sync/tool-registry.ts 補 updateShape / connectShapes / groupShapes 三個 ToolEntry 的 description 字串（per-shape-type 詳列），參考 createShape 既有格式；確保 7.1–7.3 GREEN（覆蓋 spec MODIFIED「Tool registry enumerates the full agent tool surface」）。

## 8. Frontend SSE consumer（TDD，對應 design「SSE 消費者用 fetch + ReadableStream reader（不用 EventSource）」）

- [x] 8.1 [P] 在 apps/web/src/agent/sse-parser.test.ts 撰寫 RED 測試：餵 `Uint8Array` chunks 序列含完整事件、多事件、跨 chunk 半截事件、SSE comment（`: hb`）；asyncIterable 輸出對應數量的 `AgentEvent`；comment 不 yield；對應 spec「SSE consumer parses streaming-channel events」heartbeat scenario。
- [x] 8.2 撰寫 RED 測試：multi-byte UTF-8 字元（如 `测試`）跨 chunk 切割仍正確 decode；對應 spec multi-byte scenario。
- [x] 8.3 撰寫 RED 測試：data 行 JSON 不符合 zod schema → console.warn 且不 yield；對應 spec validation 行為。
- [x] 8.4 在 apps/web/src/agent/sse-parser.ts 實作 SSE parser（用 `TextDecoder({stream: true})` + 行 buffer + blank-line dispatch + zod parse），使 8.1–8.3 GREEN。

## 9. Frontend agent thread + run hooks（TDD，對應 spec「Thread CRUD endpoints for the active user」「Thread token usage aggregation endpoint」與 ai-side-panel 各 requirement）

- [x] 9.1 [P] 在 apps/web/src/agent/useAgentThread.test.ts 撰寫 RED 測試：`useAgentThreadList(canvasId)` 回傳 `{threads, activeThreadId}`；`useCreateThread` mutation POST 後 invalidate；`useClearThread` / `useDeleteThread` 對應 endpoint；對應 spec「Thread switcher allows multi-thread navigation」create new thread + delete active thread 兩個 scenario。
- [x] 9.2 撰寫 RED 測試：`useAgentThread(threadId)` 回傳 `{thread, messages, usage: {input, output}}`；usage 來源是 GET endpoint；對應 spec「Token usage footer displays per-run and cumulative usage」total scenario。
- [x] 9.3 在 apps/web/src/agent/useAgentThread.ts 實作三個 TanStack Query hook（list / single / create+clear+delete mutations），使 9.1–9.2 GREEN。
- [x] 9.4 在 apps/web/src/agent/useAgentRun.test.ts 撰寫 RED 測試：`useAgentRun` 對 fake fetch reader 餵 SSE chunks 後 state machine 從 idle → running → done/error/cancelled 各態轉移正確；run 中 send button 不能再觸發；cancel 後不再 dispatch；對應 spec「Composer sends user message and switches to Cancel during run」雙 scenario。
- [x] 9.5 撰寫 RED 測試：done event 帶 `usage` 時 hook state 暴露 `lastRunUsage`；對應 spec「Token usage footer displays per-run and cumulative usage」this-run scenario。
- [x] 9.6 撰寫 RED 測試：fetch 回 429 + `errorKey: agent.error.rateLimited` → hook state 不進 running；對應 spec「Rate limit response shows toast with errorKey translation」scenario。
- [x] 9.7 在 apps/web/src/agent/useAgentRun.ts 實作 hook：fetch POST → reader → sse-parser → state machine + cancel button trigger POST `/api/agent/run/:runId/cancel`，使 9.4–9.6 GREEN。
- [x] 9.8 [P] 在 apps/web/src/agent/store.ts + store.test.ts 用 Zustand 實作 panel open-collapsed state + composer textarea draft；確保 toggle / collapse 行為對應 spec「Side Panel docks into Editor and is collapsible」雙 scenario。

## 10. Frontend AI Side Panel UI components（TDD 邏輯 + 視覺迭代，對應 design「AI Side Panel 走 docked flex-row layout，不浮動」「Token usage UI — this-run + thread 累計 + $ 換算，不做 enforcement」）

- [x] 10.1 [P] 在 apps/web/src/agent/ChatList.test.tsx 撰寫 RED 測試：四種 message variant（user / assistant / tool_call / tool_result）渲染對應 testid；assistant 為最後一筆且 run running 時顯示 streaming caret，terminal 後消失；對應 spec「Panel renders chat list with four message kinds」streaming caret scenario。
- [x] 10.2 在 apps/web/src/agent/ChatList.tsx 實作四種 message bubble + streaming caret 動畫（純 CSS，不用 motion 因為位於 canvas chrome 旁邊），使 10.1 GREEN。
- [x] 10.3 [P] 在 apps/web/src/agent/ChatComposer.test.tsx 撰寫 RED 測試：textarea + provider/model picker（從 useApiKeysList 預設值）+ Send button；Cmd+Enter 觸發送出；run running 時 Send 換成 Cancel + Cmd+. 觸發 cancel；in-flight 期間 Cmd+Enter 被 ignore；對應 spec「Composer sends user message and switches to Cancel during run」雙 scenario。
- [x] 10.4 在 apps/web/src/agent/ChatComposer.tsx 實作上述行為，使 10.3 GREEN。
- [x] 10.5 [P] 在 apps/web/src/agent/TokenUsageFooter.test.tsx 撰寫 RED 測試：兩列「This run」與「Total」；usage null 時 this-run 顯示 `—`；非 null 時顯示 `input X / output Y tokens, $Z`；$ 換算用 byok-pricing；< $0.0001 顯示 `<$0.0001`；對應 spec「Token usage footer displays per-run and cumulative usage」example 表四個 boundary case。
- [x] 10.6 在 apps/web/src/agent/TokenUsageFooter.tsx 實作 + 引入 `formatCost(input, output, provider, model)` 純函式（測試用），使 10.5 GREEN。
- [x] 10.7 [P] 在 apps/web/src/agent/ThreadSwitcher.test.tsx 撰寫 RED 測試：dropdown 列出 threads 依 `updated_at desc`；`+ New chat` 觸發 useCreateThread + 切 active；hover-revealed delete 觸發 useDeleteThread + 切到下一條最新 thread；對應 spec「Thread switcher allows multi-thread navigation」雙 scenario。
- [x] 10.8 在 apps/web/src/agent/ThreadSwitcher.tsx 實作 dropdown UI，使 10.7 GREEN。
- [x] 10.9 在 apps/web/src/agent/AiSidePanel.test.tsx 撰寫 RED 測試：composed component（ThreadSwitcher + ChatList + ChatComposer + TokenUsageFooter）渲染、scroll 到最底、收合按鈕 toggle；對應 spec「Side Panel docks into Editor and is collapsible」expand/collapse scenario。
- [x] 10.10 在 apps/web/src/agent/AiSidePanel.tsx 把上面四個元件組起來，使 10.9 GREEN。

## 11. Editor 整合 + Cursor AI Badge（design「AI Side Panel 走 docked flex-row layout」「Cursor AI Badge via tldraw `instancePresence.userMeta.aiActive`」）

- [x] 11.1 修 apps/web/src/canvas/Editor.tsx 把外層改成 `flex flex-row`，左側 `<Tldraw>` flex-1 min-w-0、右側根據 store 的 panel-open 狀態 conditional render `<AiSidePanel>`（width 384px）；effectiveRole === 'viewer' 時不渲染 AI panel + 不掛 toggle；以 Editor.test.tsx 加 RED→GREEN 雙 scenario：viewer 看不到 toggle、editor 點 toggle 展開/收合 panel；對應 spec「Side Panel docks into Editor and is collapsible」雙 scenario。
- [x] 11.2 修 apps/web/src/chrome/TopBar.tsx 加 AI panel toggle button（位於 Share button 旁），bind store 的 panel-open；effectiveRole==='viewer' 時不渲染；TopBar.test.tsx 對應斷言。
- [x] 11.3 [P] 在 apps/web/src/agent/cursor-ai-badge.test.ts 撰寫 RED 測試：當 `useAgentRun` state 為 running 時，hook 寫入 `editor.user.updateUserPreferences({ ...userMeta, aiActive: true })`（或對應 tldraw API）；terminal 時 reset false；對應 spec「Cursor AI Badge surfaces aiActive presence flag」multi-tab visibility scenario 的 state 設定行為。
- [x] 11.4 在 apps/web/src/agent/cursor-ai-badge.ts 實作 `useCursorAiBadge(editor, runState)` hook，使 11.3 GREEN。
- [x] 11.5 修 apps/web/src/canvas/CollaboratorAvatars.tsx 加入 `aiActive` 欄位讀取；對 aiActive===true 的 avatar 加上 gradient golden border + sparkle SVG overlay；CollaboratorAvatars.test.tsx 加 RED→GREEN 測試：兩個 collaborator presence 一個 aiActive=true / 一個 false → 只有 true 的有 overlay testid；對應 spec「Cursor AI Badge surfaces aiActive presence flag」multi-tab + disconnect 兩個 scenario。
- [x] 11.6 修 apps/web/src/canvas/use-sync-store.ts 確保 `aiActive` flag 在 instancePresence userMeta 內 round-trip（local set 後遠端能讀到、disconnect 後遠端 row 自然消失）；以 use-sync-store.test.ts 對應斷言驗證 round-trip 行為。

## 12. i18n keys（zh-TW + en 同步）

- [x] 12.1 [P] 在 packages/shared/src/locales/zh-TW.json 新增 `agent.panel.*`（toggleOpen / toggleClose / placeholder / send / cancel / clearThread / deleteThread / newChat / streamingCaret），`agent.usage.*`（thisRun / threadTotal / inputTokens / outputTokens / cost / costSubCent），`agent.badge.*`（aiEditingTooltip），`agent.thread.*`（fallbackTitleEmpty）；i18n-audit.test.ts pass（對應 spec「Rate limit response shows toast with errorKey translation」需確認既有 errorKey 翻譯仍存在）。
- [x] 12.2 [P] 在 packages/shared/src/locales/en.json 同步上述 keys。

## 13. E2E（寬定義 4 條 Playwright，對應 design 全部使用者流程）

- [x] 13.1 在 e2e/agent-cancel.spec.ts 寫 Playwright：editor 用 BYOK key 觸發「create 3 markdown shape」prompt，run 中半途按 Cancel button → SSE 收到 cancelled error → 已建立的 shape 留在 canvas + composer 回 Send 狀態；對應 spec「Composer sends user message and switches to Cancel during run」cancel cleanly scenario。
- [x] 13.2 在 e2e/agent-multi-tab-badge.spec.ts 寫 Playwright：兩個 browser context 各自 login 不同 user 同一張 canvas，user A tab 觸發 run、500ms 內 user B tab 看到 user A avatar 帶 ✨ overlay；run 結束後 500ms 內 overlay 消失；對應 spec「Cursor AI Badge surfaces aiActive presence flag」multi-tab visibility scenario。
- [x] 13.3 在 e2e/agent-viewer-no-panel.spec.ts 寫 Playwright：用 view-mode public link 進入 canvas → AI panel toggle button DOM 不存在 + 任何 panel mount 不存在；對應 spec「Side Panel docks into Editor and is collapsible」viewer hides scenario。
- [x] 13.4 在 e2e/agent-rate-limit-toast.spec.ts 寫 Playwright：用 BYOK key 1 分鐘內連送 6 次 prompt，第 6 次 toast 顯示對應 locale 翻譯（zh-TW: 「短時間內 AI 請求次數過多，請稍候再試。」）+ composer 文字保留 + 不進入 streaming；對應 spec「Rate limit response shows toast with errorKey translation」scenario。

## 14. Quality gates + 收尾

- [x] 14.1 [P] 執行 `bun run typecheck` 全綠；確認 agent 目錄 + threads 目錄無 any、所有 deps interface 顯式宣告。
- [x] 14.2 [P] 執行 `bunx oxlint apps/web/src/agent apps/api/src/agent/threads apps/api/src/agent/title-gen.ts packages/shared/src/agent-events.ts` + `bunx oxfmt --check` 全綠。
- [x] 14.3 執行 `bun test --coverage`；agent 目錄（含 web 與 api 兩側）目標 70% lines / branches；補測 gap 至達標。
- [x] 14.4 執行 `bun x playwright test`；M14 範圍 4 條 agent e2e 結果：3 條 pass（cancel / rate-limit-toast / viewer-no-panel）+ 1 條 `test.skip` pending M15 broadcast（multi-tab-badge，issue #16）。既有 happy-path e2e 維持綠（agent-viewer-no-panel 從原本 fail 修綠後，全套 10 passed / 6 skipped / 0 fail）。詳見 §22 helper 與啟用流程。
- [x] 14.5 在 docs/PHASE2_MILESTONES.md 把 M14 標記 ✅ + 註明對應 v0.5.0 release（透過 review markdown 內容確認狀態欄已更新、版號對應仍正確）。

## 15. SSE 連線存活修補（verification 階段 ingest，對應 design「SSE 連線 idle timeout override + production heartbeat」與 spec streaming-channel ADDED「SSE connection survives gaps between events」）

- [x] 15.1 SSE run handler 在 `apps/api/src/index.ts` 進入 `/api/agent/canvas/:id/run` 後第一行呼叫 `srv.timeout(req, 255)` 把該請求 Bun.serve 預設 10 s idle timeout 拉到 Bun ceiling 255 s，落實 spec ADDED Requirement「SSE connection survives gaps between events」的第一層保護（覆蓋 spec scenario「Model deliberation longer than 30 seconds does not close the stream」與 design「SSE 連線 idle timeout override + production heartbeat」）；以手動驗證（kick off run + Chrome devtools network panel 觀察 SSE 連線在 30+ 秒無事件期間維持 200 status）+ apps/api/src/agent/sse-endpoint.test.ts 既有 lifecycle test 仍綠雙重確認。
- [x] 15.2 `apps/api/src/agent/sse-endpoint.ts` 的 run handler ReadableStream.start 中呼叫 `writer.startHeartbeat()` 一次 + `setInterval(() => writer.tickHeartbeat(), 5_000)` 驅動定時 tick；`finally` 中 `clearInterval` 避免 leak（覆蓋 spec scenario「Heartbeat ticker is wired into the production run handler」）。以既有 `apps/api/src/agent/streaming.test.ts` 28 個 heartbeat test 仍綠 + apps/api/src/agent/sse-endpoint.test.ts 不退步雙重確認。
- [x] 15.3 `scripts/dev-proxy.ts` 對 `/api/agent/canvas/:id/run` 路徑套相同 `srv.timeout(req, 255)` override，避免 proxy hop 10 s 砍流（覆蓋 spec scenario「Dev proxy preserves the idle-timeout override on the run route」）。以手動驗證：dev `bun run dev` 起來後同樣 30 s+ deliberation case 透過 :3002 proxy 也能維持 SSE 連線確認。

## 16. Runtime system prompt 注入（verification 階段 ingest，對應 design「Runtime 注入 system prompt with canvas digest」與 spec agent-runtime ADDED「Runtime prepends a system prompt with canvas digest at run start」）

- [x] 16.1 在 `apps/api/src/sync/mutator-readers.ts` export internal-only reader `listAllShapes(deps, canvasId)`：回傳 `ReaderResult<ShapeSummary[]>`，房間不存在時回 `errors.devMutate.canvasNotInActiveRoom`，空 room 回 `{ok: true, data: []}`，否則回每筆 shape 的 summary 列表。**不**在 tool registry 註冊（LLM 看不到）。以 `apps/api/src/sync/mutator-readers.test.ts` 三個 listAllShapes scenario（room missing / empty / multi-shape）綠驗證；覆蓋 spec scenario「Canvas digest lists existing shapes with id, type, and geometry」的資料來源。
- [x] 16.2 在 `apps/api/src/agent/system-prompt.ts` export `buildSystemPrompt({ shapes, canvasBounds, stateAvailable })` 純函式：回傳 (a) 靜態 behavioural discipline 段（read-before-write / 不疊 / 不謊報 / 完成具體說明）+ (b) 動態 canvas digest（empty / available / unavailable 三分支，超過 40 個 shape 截斷並提示 listShapesInViewport）。以 `apps/api/src/agent/system-prompt.test.ts` 九個 scenario（discipline 四個 + digest 五個）綠驗證；覆蓋 spec scenarios「System prompt prepended to every provider call」「System prompt flags state as unavailable when the room is missing」「Canvas digest lists existing shapes with id, type, and geometry」「System prompt is not persisted to the thread」。
- [x] 16.3 在 `apps/api/src/agent/runtime.ts` `runAgent` 開頭呼叫 `listAllShapes(deps.digestDeps, canvasId)` + `getCanvasBounds(deps.digestDeps, canvasId)`，組成 `buildSystemPrompt` 輸入，將回傳字串作為 `{role: "system", content}` prepend 為 `conversation[0]`；reader 失敗時 `stateAvailable: false` 仍能 build coherent prompt。以 runtime.test.ts Group A0「System prompt prepended to conversation」兩個 scenario 綠 + 既有 120 個 agent test 不退步驗證；覆蓋 spec ADDED「Runtime prepends a system prompt with canvas digest at run start」。
- [x] 16.4 在 `apps/api/src/agent/wiring.ts` 新加 helper `extractSystem(messages)` 把 messages 陣列裡 role=system 的 entry 拉出 join 成單一字串；`buildVercelProviderAdapter.run` 改用 `streamText({ system, messages: toModelMessages(rest), tools, abortSignal })` 而不是 `streamText({ messages: toModelMessages(input.messages) })`，消除 AI SDK warning「System messages in the prompt or messages fields can be a security risk」。以 typecheck + 既有 120 個 agent test 仍綠 + 手動驗證 dev console 無此 warning 確認。

## 17. Geo shape 支援擴充（verification 階段 ingest，對應 design「Tool description per-shape-type — 加入 tldraw `geo` shape 支援」與 spec server-mutation-bridge ADDED「Mutator backfills tldraw geo required props on createShape」「Mutator converts text patches to richText on updateShape against geo shapes」+ MODIFIED「Tool registry enumerates the full agent tool surface」geo scenarios）

- [x] 17.1 在 `apps/api/src/sync/geo-defaults.ts` export `withGeoCreateDefaults(userProps)` 與 `transformGeoPartialProps(userProps)` 兩個純函式：create-side 補齊 11 個 tldraw 必填 prop（geo / dash / fill / size / font / align / verticalAlign / labelColor / url / growY / scale / w / h） + 把 `text?: string` 轉成 `richText` via tldraw 的 `toRichText` 並 drop `text` key；update-side 只轉 `text` → `richText`，**不**注入 defaults（避免覆蓋 existing record）。以 `apps/api/src/sync/geo-defaults.test.ts` 八個 scenario（withGeoCreateDefaults 四個 + transformGeoPartialProps 四個）綠驗證。
- [x] 17.2 在 `apps/api/src/sync/mutator.ts` `applyOne` 的 createShape case 對 `op.payload.type === "geo"` 套 `withGeoCreateDefaults`；updateShape case 對 `existing.type === "geo" && partial.props` 套 `transformGeoPartialProps`；非 geo shape 不動。以 `apps/api/src/sync/mutator.test.ts` 五個 geo scenario（create empty / create with user props / create non-geo unchanged / update text→richText / update non-text passthrough）綠 + 既有 17 個 mutator test 不退步驗證；覆蓋 spec ADDED「Mutator backfills tldraw geo required props on createShape」「Mutator converts text patches to richText on updateShape against geo shapes」。
- [x] 17.3 在 `apps/api/src/sync/tool-registry.ts` `CREATE_SHAPE_DESCRIPTION` 加 `type='geo'` 條目：列 20 種 variant（rectangle / ellipse / triangle / diamond / pentagon / hexagon / octagon / star / rhombus / rhombus-2 / oval / trapezoid / arrow-right / arrow-left / arrow-up / arrow-down / x-box / check-box / heart / cloud）、12 種 color、4 種 fill（none / semi / solid / pattern）、4 種 dash、size、optional `text`、各預設值；`updateShape.description` 加 geo patch.props 條目。以 `apps/api/src/sync/tool-registry.test.ts` 兩個新 geo description scenario 綠 + 既有 11 個 tool-registry test 仍綠驗證；覆蓋 spec MODIFIED 既有 createShape / updateShape description scenarios 對 geo 的擴充。

## 18. ChatComposer provider/model picker + empty state（verification 階段 ingest，對應 spec ai-side-panel MODIFIED Requirement「Composer sends user message and switches to Cancel during run」picker scenarios）

- [x] 18.1 在 `apps/web/src/agent/ChatComposer.tsx` 把原 read-only `provider / model` span 換成兩個 dropdown：provider dropdown 列 `availableProviders` 全部 entry、model dropdown 由 `BYOK_PRICING` filter `providerId === selectedProvider` 取得；切換 provider 時自動 reset model 為 `preferences[provider]?.model` 或 fallback 至該 provider 第一筆 pricing。以 `apps/web/src/agent/ChatComposer.test.tsx` 四個新 picker scenario（render provider dropdown / filter model by provider / switch provider resets model / switch model overrides）綠驗證；覆蓋 spec scenarios「Provider dropdown lists every configured provider」「Model dropdown is filtered to the selected provider」「Changing the provider resets the model to that provider's preference」「Changing the model on the picker overrides the preference for this submission」。
- [x] 18.2 在 ChatComposer 對 `availableProviders.length === 0` 狀態：textarea / Send button disabled、provider dropdown 與 model dropdown 不掛載 DOM、改 render `data-testid="chat-composer-no-api-key-hint"` 提示元素引用 i18n key `agent.panel.noApiKeyHint`。以 ChatComposer.test.tsx 兩個新 scenario（textarea disabled + hint visible / 兩個 dropdown null）綠驗證；覆蓋 spec scenario「Composer fully disabled when no BYOK key is configured」。
- [x] 18.3 在 `packages/shared/src/locales/zh-TW.json` 與 `en.json` `agent.panel.*` 區同步新增三個 key：`providerLabel`、`modelLabel`、`noApiKeyHint`。以既有 `packages/shared/src/locales/i18n-audit.test.ts` 仍綠 + ChatComposer.test.tsx 對 `noApiKeyHint` testid 可見驗證；覆蓋 hard rule「每個 UI string 都走 t('key') + zh-TW/en 同步」。

## 19. Provider adapter token usage extraction（verification 階段 ingest，對應 design decision「Provider adapter token usage extraction（M14 verification fix）」與 spec agent-runtime ADDED Requirement「Provider adapter extracts token usage from finish-step events」）

- [x] 19.1 在 `apps/api/src/agent/wiring.ts` 把 `bridge()` 內部的 part-to-event 邏輯抽成 module-level pure helper `aiSdkPartToProviderEvents(part): ProviderEvent[]` 並 export；`bridge()` 改成迴圈呼叫該 helper 並 yield 所有結果（`error` part 例外仍直接 throw `ProviderError`）。以 `apps/api/src/agent/wiring.test.ts` 新增的單元測試（text-delta / tool-call / finish-step 各 case）綠驗證；覆蓋 spec ADDED Requirement「Provider adapter extracts token usage from finish-step events」結構面要求（pure helper 可被 unit test）。
- [x] 19.2 `aiSdkPartToProviderEvents` 對 `finish-step` 永遠 emit `{ type: "step-finish", finishReason }`；當 `usage` 物件存在且 `inputTokens` / `outputTokens` 至少一個是 `number` 時，**額外** emit `{ type: "usage", usage: { input, output } }`（缺的一側填 0）；兩者都 undefined 或無 usage 物件 → 不 emit usage event 以保留 runtime 既有的 "provider returned no usage information" warning path。以 wiring.test.ts 五個 finish-step 相關 scenario 綠驗證；覆蓋 spec scenarios「finish-step with both token counts emits step-finish then usage」「finish-step without a usage object emits step-finish only」「finish-step with both token counts undefined omits the usage event」「finish-step with partial usage fills the missing side with zero」。
- [x] 19.3 既有 `apps/api/src/agent/runtime.ts` 的 usage 累加器（step-by-step 加總）保持不動，因為 runtime 既有累加邏輯已正確；fix 純粹是補上 wiring 邊界漏掉的事件轉換。以既有 130+ 個 agent test 仍綠驗證（runtime / sse-endpoint / integration） — 沒有 regression。

## 20. Assistant bubble Markdown 渲染（verification 階段 ingest，對應 design decision「Markdown rendering for assistant bubbles（M14 verification feature）」與 spec ai-side-panel ADDED Requirement「Assistant bubbles render markdown; user bubbles render plain text」）

- [x] 20.1 在 `apps/web/package.json` 加入 `react-markdown@10` + `remark-gfm@4` 兩個 runtime 依賴（合計 ~30 KB gzipped）；以 `bun install` 完成後 lockfile 更新確認；翻過 design.md 的 non-goal「不引入新 npm package」並在 design Decision「Markdown rendering for assistant bubbles」紀錄理由。
- [x] 20.2 在 `apps/web/src/agent/ChatList.tsx` 新增 `AssistantMarkdown` 子元件 — 包 `ReactMarkdown remarkPlugins={[remarkGfm]}` + Tailwind `prose prose-sm max-w-none` + arbitrary variants 設 padding / margin / code 背景；把 `MessageBubble`（role === "assistant"）與 streaming bubble 內的純文字渲染都換成 `<AssistantMarkdown text={...} />`；user bubble 與 tool bubble 維持原樣。落實 spec ADDED Requirement「Assistant bubbles render markdown; user bubbles render plain text」的差別渲染合約。以 `apps/web/src/agent/ChatList.test.tsx` 五個新 scenario（bold → `<strong>` / list → `<ul>` `<li>` / inline code → `<code>` / user bubble plain text / streaming bubble markdown）綠驗證；覆蓋 spec scenarios「Assistant Markdown bold renders as a strong element」「Assistant Markdown list renders as a list element」「Assistant inline code renders as a code element」「User bubble preserves Markdown syntax as plain text」「Streaming bubble renders Markdown progressively」。
- [x] 20.3 確認 `@tailwindcss/typography` plugin 已在 `apps/web/src/styles.css` `@plugin "@tailwindcss/typography"` 啟用，`prose` class 與 arbitrary variant 樣式都生效；以手動驗證重啟 dev 後 chat list 中 AI 回覆的 markdown 正常顯示為 DOM 元素 + 樣式可見確認。

## 21. Agent error toast UI + composer draft preservation（verification 階段 ingest，對應 design decision「Agent error toast surfaced inline in AiSidePanel（M14 verification fix）」「Composer draft cleared only on `done` terminal — preserved on error/cancelled（M14 verification fix）」與 spec ai-side-panel MODIFIED Requirement「Rate limit response shows toast with errorKey translation」）

- [x] 21.1 在 `apps/web/src/agent/AiSidePanel.tsx` 新增 `useState<string | null>` 持有 `toastErrorKey` + `useEffect` 監 `run.state === "error" && run.error` → `setToastErrorKey(run.error)` + 6 秒 auto-dismiss timer。Toast 渲染為 inline 元素（`<aside>` 內 `absolute right-4 top-4`），含 `data-testid="agent-error-toast"`、`role="alert"`、`✕` 手動 dismiss、`t(toastErrorKey)` i18n 翻譯；落實 spec MODIFIED Requirement「Rate limit response shows toast with errorKey translation」的 inline transient toast 行為。以 e2e `agent-rate-limit-toast.spec.ts` 第 6 次 send 觸發 toast 可見 + 含中英 locale 翻譯文字驗證。
- [x] 21.2 在 `apps/web/src/agent/ChatComposer.tsx` 把 `setDraft("")` 從同步 submit 內移除；新增 `useEffect` + `useRef<AgentRunState>` 監 `prevState === "running" && state === "done"` 才 clear；error / cancelled terminal preserve typed text。落實 spec scenario「Draft preserved on error terminal, cleared on done terminal」。以 ChatComposer.test.tsx 既有 12 個 test 仍綠 + e2e `agent-rate-limit-toast.spec.ts` 最後 assertion `await expect(composer).toHaveValue("hi 6")` 通過驗證。

## 22. E2E sharing helper + 4 agent specs 真實啟用（verification 階段 ingest，對應 design decision「E2E sharing helper + four agent specs activated（M14 verification fix）」與既有 §13 e2e 規劃落地）

- [x] 22.1 新加 `e2e/helpers/agent-setup.ts` export 五個 helper：`pickEnabledByokKey()`（讀 `E2E_TEST_USER_BYOK_GEMINI` 或 `_OPENAI`，accept Gemini OR OpenAI 任一）、`signInWithMagicLink(page, email)`（Mailpit verify URL → /dashboard）、`createCanvasViaDashboard(page, title)`（點 Create canvas → 填 dialog → click card 進 canvas page）、`injectByokKey(request, provider, apiKey)`（POST `/api/account/byok/{provider}` validate-then-encrypt）、`setupAgentUserAndCanvas(browser, byok, opts)` 一體化 wrapper。以 `bun --filter '*' typecheck` 三 package exit 0 + 後續 e2e spec 引用此 helper 順利 sign-in / create canvas / inject BYOK 驗證。
- [x] 22.2 `e2e/agent-cancel.spec.ts` 從 `test.fixme(true, ...)` 改為 helper-driven 真實 cancel 流程：fill 長 reasoning prompt → 等 Cancel button visible（30 s timeout） → click Cancel → 等 Send button 回（15 s timeout）。落實 ai-side-panel spec scenario「Cancel ends the run cleanly」的 e2e 覆蓋。以 `bun x playwright test e2e/agent-cancel.spec.ts` 1.5 s pass 確認 cancel SSE 路徑通。
- [x] 22.3 `e2e/agent-rate-limit-toast.spec.ts` 從 `test.fixme(true, ...)` 改為 hybrid API + UI：用 `page.request.post` 連發 5 個 quota-burn run → 用 UI fill `"hi 6"` + click Send → 等 `agent-error-toast` testid visible + 包含 locale 翻譯 + composer preserve `"hi 6"`。落實 spec scenario「Sixth request inside the 60-second window」+「Draft preserved on error terminal, cleared on done terminal」。以 `bun x playwright test e2e/agent-rate-limit-toast.spec.ts` 12 s pass 確認 server 429 + UI toast + draft preserve 三條 assertion 都過。
- [x] 22.4 `e2e/agent-viewer-no-panel.spec.ts` 既有實作的 dashboard dialog flow + share API（M14 verification 中已修四處：i18n regex 兩語都接 / 等 dialog 出現後 fill 再 submit / 用 `a[href^="/canvas/"]` locator click card / 用 PUT `/api/canvas/:id/share/link` + GET `/api/canvas/:id/share` 替代 PATCH + 自構 `?share=token` URL）持續綠。以 `bun x playwright test e2e/agent-viewer-no-panel.spec.ts` pass 確認 viewer 看不到 panel toggle。
- [x] 22.5 `e2e/agent-multi-tab-badge.spec.ts` body 寫齊（u1 invite via share API → u2 sign-in + accept invite from Mailpit → u1 dispatch run → u2 等 ✨ overlay），但 `test.skip(true, "M15 — cross-tab broadcast pending (#16). Spec body kept ready.")` — `cursor-ai-badge.ts` 寫 `TLInstance.meta` 是 local-only 不跨 tab broadcast，需 M15 改 `TLInstancePresence.meta`。對應 memory `project_ai_badge_cross_tab_debt.md` 與 issue #16 acceptance criteria。
- [x] 22.6 `e2e/sharing-acceptance.spec.ts` 直接 `git rm`：自身 line 7 註解明寫「Not a regression test — assertions are minimal and only enough to keep the scenario on-track. The screenshots are the deliverable」，視覺驗收截圖 spec、跟 M14 / 分享功能 regression 無關；share-invite.spec.ts + share-public-link.spec.ts 兩條 functional spec 仍綠覆蓋分享功能 regression path。以 `bun x playwright test --reporter=list` 全套 17 → 16 tests，無相關 fail 驗證。
