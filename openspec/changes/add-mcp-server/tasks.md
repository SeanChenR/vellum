## 1. DB schema + drizzle 同步（落實 spec「Personal access tokens are persisted as opaque hashed entries per user」與 design「Authentication via Personal Access Token (PAT), opaque random token + DB hash」「Token last_used_at: throttled write, max once per 60 seconds」）

- [x] 1.1 在 `apps/api/src/db/schema.ts` 加 `personalAccessTokens` drizzle pgTable 欄位 `id text PRIMARY KEY` / `userId text NOT NULL REFERENCES users(id) ON DELETE CASCADE` / `name text NOT NULL` / `tokenHash text NOT NULL` / `tokenPrefix text NOT NULL` / `scope text` / `expiresAt timestamptz` / `lastUsedAt timestamptz` / `createdAt timestamptz NOT NULL DEFAULT now()` / `revokedAt timestamptz`；以 `apps/api/src/db/schema.test.ts` 對應 assertion 驗 column / FK / cascade。落實 spec ADDED Requirement「Personal access tokens are persisted as opaque hashed entries per user」shape。
- [x] 1.2 [P] 在同檔加兩個 index：`patUserIdx ON personal_access_tokens (user_id, created_at DESC)` 與 `patHashActiveIdx UNIQUE ON personal_access_tokens (token_hash) WHERE revoked_at IS NULL`；以 schema.test.ts 對應 assertion + spec scenario「Unique partial index permits a revoked hash to be reused」cover。落實 spec ADDED Requirement「Personal access tokens are persisted as opaque hashed entries per user」index 部分。
- [x] 1.3 透過 `bunx drizzle-kit generate` 產出 `apps/api/drizzle/0007_personal_access_tokens.sql`；review SQL 確認 cascade FK 正確、partial unique index 條件 `WHERE revoked_at IS NULL` 在；以 `bunx drizzle-kit migrate` apply 後在 dev DB 用 psql 確認表 + 2 個 index 存在。

## 2. PAT token 格式 + repo（TDD 縱切，對應 spec「Personal access tokens are persisted as opaque hashed entries per user」實作面 + design decision「Authentication via Personal Access Token (PAT), opaque random token + DB hash」）

- [x] 2.1 [P] 在 `apps/api/src/pat/token-format.test.ts` 撰寫 RED 測試：`generateTokenPlaintext()` 回 `vlm_pat_` 前綴 + 32 base62 char（總 40 char）；`hashTokenPlaintext(plaintext)` 回 SHA-256 hex；`extractTokenPrefix(plaintext)` 回前 12 char；對應 spec scenario「Inserting a token stores the SHA-256 hash and not the plaintext」與 design「Authentication via Personal Access Token (PAT), opaque random token + DB hash」格式。
- [x] 2.2 在 `apps/api/src/pat/token-format.ts` 實作上述三個函式（用 `crypto.getRandomValues` + base62 表 / Bun.CryptoHasher 或 node:crypto 計算 hash），使 2.1 GREEN。
- [x] 2.3 [P] 在 `apps/api/src/pat/repo.test.ts` 撰寫 RED 測試：`createToken(userId, {name, expiresInDays})` 寫入 DB row 含 hash + prefix 並回 plaintext + row metadata；`listTokens(userId)` 回 active tokens 依 `created_at DESC`；`revokeToken(userId, tokenId)` 對 owner 寫 `revoked_at`；對 cross-user 拒；對 unknown id 拒；對應 spec scenarios「List endpoint returns user's tokens in reverse creation order」「Owner deletes their own token」「Cross-user delete returns 404 to avoid existence leak」。
- [x] 2.4 在 `apps/api/src/pat/repo.ts` 實作上述函式 + `findActiveTokenByHash(hash)` 對 `revoked_at IS NULL` 且 `(expires_at IS NULL OR expires_at > NOW())` 行查 user_id；使 2.3 GREEN；額外 unit test cover scenario「Authenticating with a revoked token fails」的 repo 層返回 null。

## 3. PAT authenticator（落實 spec「PAT authenticator resolves token plaintext to user id without exposing hashes」與 design「Token last_used_at: throttled write, max once per 60 seconds」）

- [x] 3.1 [P] 在 `apps/api/src/pat/auth.test.ts` 撰寫 RED 測試：`authenticatePat(req, deps)` 從 `Authorization: Bearer <token>` header 解 plaintext → `hashTokenPlaintext` → `repo.findActiveTokenByHash` → 回 `{userId, tokenId}` 或 `null`；missing header / wrong prefix / unknown hash / expired / revoked 一律回 null；plaintext 不進任何 log 行（用 fake logger spy 驗）；scenario「Authenticator never logs plaintext」直接斷言；落實 spec ADDED Requirement「PAT authenticator resolves token plaintext to user id without exposing hashes」。
- [x] 3.2 在 `apps/api/src/pat/auth.ts` 實作 `authenticatePat`；用 fake logger 在 test 驗證 log 行只含 token id + `prefix.slice(-4)`；使 3.1 GREEN；落實 spec ADDED Requirement「PAT authenticator resolves token plaintext to user id without exposing hashes」實作面。
- [x] 3.3 [P] 在同檔加 `touchLastUsed(tokenId, repo, clock, throttleMap)` helper：用 in-memory LRU map（capacity 10k，跟 RateLimiter 同層級）做 60 s throttle；第二次同 token 在 30 s 內呼叫 → skip UPDATE；超過 60 s → UPDATE；以 auth.test.ts 對應兩個 scenario「Two requests within 60 seconds produce one DB write」「Requests 60+ seconds apart produce separate writes」綠驗證；落實 spec ADDED Requirement「Last-used tracking updates at most once per 60 seconds per token」與 design decision「Token last_used_at: throttled write, max once per 60 seconds」。

## 4. PAT REST endpoints（TDD，對應 spec「GET /api/account/pat lists the signed-in user's active tokens」「POST /api/account/pat creates a token and returns the plaintext exactly once」「DELETE /api/account/pat/:id revokes a token via soft delete」）

- [x] 4.1 在 `apps/api/src/pat/routes.test.ts` 撰寫 RED 測試：`GET /api/account/pat` 對 authenticated user 回 200 + `{data: [...]}` 不含 plaintext / token_hash；unauthenticated 401；落實 spec ADDED Requirement「GET /api/account/pat lists the signed-in user's active tokens」與 scenarios「List endpoint returns user's tokens in reverse creation order」「List endpoint omits token_hash and plaintext」「Unauthenticated GET returns 401」。
- [x] 4.2 撰寫 RED 測試：`POST /api/account/pat` 對 `{name, expiresInDays?}` 回 201 + `{data: {token: plaintext, id, name, prefix, expiresAt, createdAt}}`；`expiresInDays: 90` → `expires_at` 在 90 天後；`expiresInDays: null` 或 omitted → `expires_at = null`；missing `name` → 400 `errors.validation`；name > 64 chars → 400；落實 spec ADDED Requirement「POST /api/account/pat creates a token and returns the plaintext exactly once」與 scenarios「Successful creation returns plaintext and id」「expiresInDays null means never expires」「Invalid body returns 400 invalidPayload」「Token name accepts up to 64 chars」。
- [x] 4.3 撰寫 RED 測試：`DELETE /api/account/pat/:id` 對 owner 回 204 + DB `revoked_at IS NOT NULL`；cross-user 回 404；non-existent id 回 404；落實 spec ADDED Requirement「DELETE /api/account/pat/:id revokes a token via soft delete」與 scenarios「Owner deletes their own token」「Cross-user delete returns 404 to avoid existence leak」「Non-existent id returns 404」。
- [x] 4.4 在 `apps/api/src/pat/routes.ts` 實作三個 handler 注入 `PatRepo` deps + session（從既有 `getSession(req)` better-auth path 取 userId）；使 4.1–4.3 GREEN。
- [x] 4.5 在 `apps/api/src/index.ts` 掛載 `/api/account/pat` + `/api/account/pat/:id` 兩條路由 + 套用既有 session middleware；以 integration test 驗證一條完整 path（authenticated POST → GET 看到剛建的 token row → DELETE → GET 不再列出）。

## 5. listCanvases reader + tool registry 第 12 個 entry（落實 spec server-mutation-bridge MODIFIED Requirement「Tool registry enumerates the full agent tool surface」12 個 entry 與 design「12th tool: listCanvases (read kind), exposed only via MCP entry point in the sense that in-process never calls it」「Per-user token, canvasId on every tool call (GitHub PAT model)」listCanvases 例外特化）

- [x] 5.1 [P] 在 `apps/api/src/sync/list-canvases-reader.test.ts` 撰寫 RED 測試：`listCanvasesForUser(deps, userId)` query `canvases` join `canvas_shares` 回 user owned + shared canvases，每筆含 `{id, title, role}`，role 為 `owner|editor|viewer`；空集合回 `{ok: true, data: []}`；落實 spec mcp-server scenario「listCanvases bypasses per-canvas check and returns user's accessible canvases」資料層。
- [x] 5.2 在 `apps/api/src/sync/list-canvases-reader.ts` 實作 `listCanvasesForUser(deps, userId)` reader，使 5.1 GREEN；上限回 100 筆（防 enterprise 量級）。
- [x] 5.3 在 `apps/api/src/sync/tool-registry.ts` 加第 12 個 entry `listCanvases`：`kind: "read"`、`schema: z.object({}).strict()`、description 含「list canvases」「accessible」「role」三個關鍵字、`execute` 內呼叫 `listCanvasesForUser(deps, deps.session.userId)` 並 ignore canvasId 參數；落實 spec server-mutation-bridge MODIFIED Requirement「Tool registry enumerates the full agent tool surface」12-entry shape + scenarios「Registry enumerates exactly twelve entries with correct kinds」「listCanvases description signals user-scoped discovery」「listCanvases execute routes through listCanvasesForUser ignoring canvasId」。
- [x] 5.4 [P] 在 `apps/api/src/sync/tool-registry.test.ts` 擴充 RED 測試：`Object.values(toolRegistry)` length === 12；write kind 6 個 + read kind 6 個（含 listCanvases）；description 含 user-scoped 關鍵字；execute 對任意 canvasId 都呼叫 reader 拿 userId 走 path；使測試綠；落實 spec server-mutation-bridge MODIFIED Requirement「Tool registry enumerates the full agent tool surface」test 覆蓋。

## 6. MCP JSON-RPC parser + zod-to-json-schema converter（落實 spec mcp-server ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」protocol error 部分 + ADDED Requirement「Zod-to-JSON-Schema conversion is in-tree and snapshot-tested per tool」 + design「JSON-RPC error mapping: -32700/-32600/-32601 protocol, -32000 application + errorKey data」「Zod-to-JSON-Schema conversion: 自寫小型 converter」）

- [x] 6.1 [P] 在 `apps/api/src/mcp/jsonrpc.test.ts` 撰寫 RED 測試：`parseJsonRpcRequest(body)` 對 valid `{jsonrpc, id, method, params?}` 回 parsed；malformed JSON 回 `{error: {code: -32700}}`；缺 `jsonrpc` / 缺 `method` 回 `{error: {code: -32600}}`；落實 spec mcp-server scenarios「Malformed JSON body returns -32700 parse error」「Invalid JSON-RPC envelope returns -32600」與 ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」protocol error 路徑。
- [x] 6.2 在 `apps/api/src/mcp/jsonrpc.ts` 實作 `parseJsonRpcRequest` + `makeJsonRpcSuccess(id, result)` + `makeJsonRpcError(id, code, message, data?)`，使 6.1 GREEN。
- [x] 6.3 [P] 在 `apps/api/src/mcp/zod-to-json-schema.test.ts` 撰寫 RED 測試：對 12 個 tool 的 Zod schema 各做一個 snapshot test — `createShapePayloadSchema` 轉出 `{type: "object", required: ["id","type","x","y","props"], additionalProperties: false}`；`updateShapePayloadSchema` (discriminated union) 轉出對應 oneOf；`getShape` 轉 `{required: ["shapeId"]}`；其他 9 個各自；落實 spec mcp-server ADDED Requirement「Zod-to-JSON-Schema conversion is in-tree and snapshot-tested per tool」與 scenarios「All twelve tool schemas convert to valid JSON Schema」「createShape Zod schema converts to JSON Schema with correct required fields」。
- [x] 6.4 在 `apps/api/src/mcp/zod-to-json-schema.ts` 實作 in-tree converter 支援 design decision 列出的 Zod feature 子集（`z.object().strict()`、`z.string()`、`z.string().regex()`、`z.string().min()`、`z.number().finite()`、`z.number().int()`、`z.literal()`、`z.enum()`、`z.array()`、`z.record()`、`z.optional()`、`z.nullable()`、`z.discriminatedUnion()`）；使 6.3 GREEN；落實 spec mcp-server ADDED Requirement「Zod-to-JSON-Schema conversion is in-tree and snapshot-tested per tool」實作面與 design「Zod-to-JSON-Schema conversion: 自寫小型 converter」「不引入新 npm package」原則。

## 7. MCP methods 實作（TDD，落實 spec mcp-server ADDED Requirements「initialize returns server capabilities advertising tools support」「ping returns empty result for client health checks」「tools/list returns the full Vellum tool surface as MCP tool descriptors」「tools/call dispatches to the existing tool-registry execute path with permission enforcement」與 design「M15 MVP methods: initialize / ping / tools/list / tools/call」「Permission check for tools/call: user role + tool kind 雙條件」「Per-user token, canvasId on every tool call (GitHub PAT model)」）

- [x] 7.1 [P] 在 `apps/api/src/mcp/methods/initialize.test.ts` 撰寫 RED 測試：`handleInitialize(params)` 回 `{protocolVersion: "2024-11-05", capabilities: {tools: {}}, serverInfo: {name: "vellum-mcp-server", version: <package version>}}`；client `params.protocolVersion` 不符仍回 server 自身版本；落實 spec mcp-server ADDED Requirement「initialize returns server capabilities advertising tools support」與 scenario「initialize returns server info and tools capability」。
- [x] 7.2 [P] 在 `apps/api/src/mcp/methods/initialize.ts` 實作 + `serverInfo.version` 從 `apps/api/package.json` `version` 欄位讀；使 7.1 GREEN；落實 spec mcp-server ADDED Requirement「initialize returns server capabilities advertising tools support」實作面。
- [x] 7.3 [P] 在 `apps/api/src/mcp/methods/ping.test.ts` 撰寫 RED 測試：`handlePing()` 回 `{}`；落實 spec mcp-server ADDED Requirement「ping returns empty result for client health checks」與 scenario「ping returns empty result」。
- [x] 7.4 [P] 在 `apps/api/src/mcp/methods/ping.ts` 實作（5 行），使 7.3 GREEN；落實 spec mcp-server ADDED Requirement「ping returns empty result for client health checks」實作面。
- [x] 7.5 [P] 在 `apps/api/src/mcp/methods/tools-list.test.ts` 撰寫 RED 測試：`handleToolsList()` 回 `{tools: [...]}` 12 entries；每個含 `name` / `description` / `inputSchema` (JSON Schema) / `annotations.readOnlyHint`；6 write tool readOnlyHint=false / 6 read tool=true（含 listCanvases）；createShape inputSchema 對 spec scenario「tools/list inputSchema is valid JSON Schema for createShape」直接 assert；落實 spec mcp-server ADDED Requirement「tools/list returns the full Vellum tool surface as MCP tool descriptors」與 scenarios「tools/list enumerates exactly twelve tools」「tools/list readOnlyHint annotation distinguishes write vs read」。
- [x] 7.6 [P] 在 `apps/api/src/mcp/methods/tools-list.ts` 實作：iterate `tool-registry` → 直接 mapping → `{name, description, inputSchema: zodToJsonSchema(entry.schema), annotations: {readOnlyHint: entry.kind === "read"}}`；使 7.5 GREEN；落實 spec mcp-server ADDED Requirement「tools/list returns the full Vellum tool surface as MCP tool descriptors」實作面。
- [x] 7.7 在 `apps/api/src/mcp/methods/tools-call.test.ts` 撰寫 RED 測試：`handleToolsCall(deps, session, params)` 對 valid `{name, arguments}` 走 `tool-registry[name].execute(deps, arguments.canvasId, arguments)`；result 包成 `{content: [{type: "text", text: JSON.stringify(result)}], isError: false}`；落實 spec mcp-server ADDED Requirement「tools/call dispatches to the existing tool-registry execute path with permission enforcement」與 scenario「createShape with owner role dispatches mutator」。
- [x] 7.8 撰寫 RED 測試 — permission 邊界：write tool 對 viewer role canvas 回 -32000 invalidRequest；任何 tool 對 user 沒 role 的 canvas 回 -32000 invalidRequest（不分 「不存在」/「無權限」）；落實 spec mcp-server ADDED Requirement「tools/call dispatches to the existing tool-registry execute path with permission enforcement」與 scenarios「tools/call against a canvas the user has no role on returns invalidRequest」「viewer role attempts write tool returns invalidRequest」與 design「Permission check for tools/call: user role + tool kind 雙條件」「Per-user token, canvasId on every tool call (GitHub PAT model)」runtime check。
- [x] 7.9 撰寫 RED 測試 — listCanvases bypass：`{name: "listCanvases", arguments: {}}` 不檢查 canvasId / canvas role，直接 dispatch；user own 3 + share viewer 2 → 5 entries 各帶 role；落實 spec mcp-server ADDED Requirement「tools/call dispatches to the existing tool-registry execute path with permission enforcement」listCanvases 例外 + scenario「listCanvases bypasses per-canvas check and returns user's accessible canvases」與 design「Permission check for tools/call: user role + tool kind 雙條件」listCanvases 特化。
- [x] 7.10 撰寫 RED 測試 — error mapping：unknown tool name 回 -32000 `toolUnknown`；arguments 不符 Zod 回 -32000 `invalidArgs`；mutator throw 回 -32000 + 對應 `errors.devMutate.*`；落實 spec mcp-server ADDED Requirement「tools/call dispatches to the existing tool-registry execute path with permission enforcement」error mapping + scenarios「Tool arguments failing Zod validation return invalidArgs」「Unknown tool name returns toolUnknown」與 design「JSON-RPC error mapping: -32700/-32600/-32601 protocol, -32000 application + errorKey data」。
- [x] 7.11 在 `apps/api/src/mcp/methods/tools-call.ts` 實作 `handleToolsCall`：先 permission check（listCanvases bypass / write tool owner+editor / read tool owner+editor+viewer / 用 `requireRole`） → 解 `arguments.canvasId` → 呼叫 `entry.schema.safeParse(arguments)` → 呼叫 `entry.execute(deps, canvasId, parsed)` → 包成 MCP content；使 7.7–7.10 GREEN；落實 spec mcp-server ADDED Requirement「tools/call dispatches to the existing tool-registry execute path with permission enforcement」實作面。

## 8. MCP server dispatch + endpoint mount + rate limit（落實 spec mcp-server ADDED Requirements「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」「PAT authentication gate sits in front of JSON-RPC dispatch」「MCP_TOOL_CALL_RULE rate-limits tools/call at 60 calls per 60 seconds per user」與 design「Stateless Streamable HTTP, single endpoint POST /api/mcp」「Per-user rate-limit bucket, MCP_TOOL_CALL_RULE = 60/60s」）

- [x] 8.1 在 `apps/api/src/lib/rate-limit-rules.ts` 加 `export const MCP_TOOL_CALL_RULE: RateLimitRule = { windowMs: 60_000, max: 60 }`；以 rate-limit-rules.test.ts 對應 assertion 驗。落實 spec mcp-server ADDED Requirement「MCP_TOOL_CALL_RULE rate-limits tools/call at 60 calls per 60 seconds per user」常數定義。
- [x] 8.2 [P] 在 `apps/api/src/mcp/dispatch.test.ts` 撰寫 RED 測試：`dispatchMcpRequest(deps, body, session)` 對 valid initialize / ping / tools/list / tools/call 走對應 handler；unknown method 回 -32601 method not found；落實 spec mcp-server ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」與 scenario「Unknown method returns -32601」與 design「M15 MVP methods: initialize / ping / tools/list / tools/call」。
- [x] 8.3 [P] 在 `apps/api/src/mcp/dispatch.ts` 實作 method router：switch on `method` → 對應 handler；unsupported 回 -32601；使 8.2 GREEN；落實 spec mcp-server ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」method dispatch 路徑。
- [x] 8.4 在 `apps/api/src/mcp/index.test.ts` 撰寫 RED 測試 — 整條 `POST /api/mcp` HTTP path：missing Auth header → 401；invalid token → 401；expired token → 401；revoked token → 401；落實 spec mcp-server ADDED Requirement「PAT authentication gate sits in front of JSON-RPC dispatch」與 scenarios「Missing Authorization header returns HTTP 401」「Invalid PAT returns HTTP 401」「Expired PAT returns HTTP 401」「Revoked PAT returns HTTP 401」與 ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」auth gate 部分。
- [x] 8.5 撰寫 RED 測試 — rate limit：61st tools/call within 60 s → 429 + `Retry-After` header；tools/list 不 consume bucket（即使 bucket 空 tools/list 仍回 200）；同 user 兩張 PAT 共享 bucket；落實 spec mcp-server ADDED Requirement「MCP_TOOL_CALL_RULE rate-limits tools/call at 60 calls per 60 seconds per user」與 scenarios「61st tools/call within 60 seconds returns HTTP 429」「tools/list does NOT consume the bucket」「Multiple PATs from the same user share the bucket」。
- [x] 8.6 撰寫 RED 測試 — last_used_at throttle：connected through full path，連 2 個 request 在 30 s 內 → 只 1 個 DB UPDATE；落實 spec mcp-server ADDED Requirement「Last-used tracking updates at most once per 60 seconds per token」端到端 + scenario「Two requests within 60 seconds produce one DB write」。
- [x] 8.7 在 `apps/api/src/mcp/index.ts` 實作 `handleMcpRequest(req, deps)`：authenticatePat → 401 if null → parseJsonRpcRequest → method=tools/call 才走 rate limiter `MCP_TOOL_CALL_RULE` key `mcp:user:<userId>` → 429 if not allowed → dispatchMcpRequest → makeJsonRpcSuccess/Error → 200 with body；touchLastUsed throttled；使 8.4–8.6 GREEN；落實 spec mcp-server ADDED Requirements「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」「PAT authentication gate sits in front of JSON-RPC dispatch」實作面。
- [x] 8.8 在 `apps/api/src/index.ts` 掛載 `/api/mcp` POST route 進 main Bun.serve fetch handler；以 `apps/api/src/index.test.ts` 或 integration test 驗證 endpoint reachable + 完整 init/ping/list/call 流程跑通。

## 9. Settings UI — MCP Tokens 分頁（TDD 邏輯 + 視覺迭代，落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」）

- [x] 9.1 [P] 在 `apps/web/src/account/usePatTokens.test.ts` 撰寫 RED 測試：`usePatTokens()` 從 `GET /api/account/pat` 拉 list；`useCreatePatToken()` mutation POST 後 invalidate；`useRevokePatToken()` mutation DELETE 後 invalidate；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」資料層 + ADDED Requirement「GET /api/account/pat lists the signed-in user's active tokens」client 端。
- [x] 9.2 在 `apps/web/src/account/usePatTokens.ts` 實作三個 TanStack Query hook，使 9.1 GREEN。
- [x] 9.3 [P] 在 `apps/web/src/account/PatTokensSection.test.tsx` 撰寫 RED 測試：rendering 一個 sample token row → 顯示 name / masked prefix / lastUsedAt 相對時間 / expiresAt；點 「Create token」 → dialog 開 → 填 name + 選 30 days + submit → dialog 顯示 plaintext input + Copy button + warning；close dialog 後重 render → token row 出現但無 plaintext；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」 + scenario「Create-token dialog shows plaintext exactly once」。
- [x] 9.4 撰寫 RED 測試 — revoke 流程：點 revoke icon → 確認 dialog → confirm → DELETE call → row disappears；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」 + scenario「Revoke action requires confirmation and removes the row」。
- [x] 9.5 在 `apps/web/src/account/PatTokensSection.tsx` 實作 inline section UI（react-hook-form + zod for create dialog；既有 shadcn/ui Dialog；toast for clipboard copy success；relative time format 用既有 `date-fns` 或 inline）；使 9.3–9.4 GREEN；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」實作面。
- [x] 9.6 在 `apps/web/src/account/AccountPage.tsx` mount `<PatTokensSection />` 在「API Keys」page 下方新分頁或同 page 區塊；以 AccountPage.test.tsx 對應 assertion 驗 section 可見。

## 10. i18n keys（zh-TW + en 同步，落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」scenario「i18n keys are present in both zh-TW and en」）

- [x] 10.1 [P] 在 `packages/shared/src/locales/zh-TW.json` 新增 `account.pat.*` keys：`title` / `createButton` / `nameLabel` / `expiresLabel` / `expires30Days` / `expires90Days` / `expiresNever` / `plaintextWarning` / `copyButton` / `revokeButton` / `revokeConfirm` / `lastUsedNever` / `lastUsedAt` / `expiresAt` / `expiresNeverDisplay`；以 i18n-audit.test.ts 對應 pass 驗證；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」i18n 部分。
- [x] 10.2 [P] 在 `packages/shared/src/locales/en.json` 同步上述 keys；落實 spec personal-access-token ADDED Requirement「Settings UI presents a MCP Tokens panel for token CRUD」i18n 雙語同步。

## 11. Cursor AI Badge 跨 tab broadcast 修補（M14 verification debt 併入 M15，落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」與 design「Cursor AI Badge: 寫進 TLInstancePresence.meta via editor.store.put + InstancePresenceRecordType.createId(userId)」）

- [x] 11.1 在 `apps/web/src/agent/cursor-ai-badge.test.ts` 改既有 test：fake editor 暴露 `store.get` + `store.put` + `user.getId` narrow surface；當 `useAgentRun` state 為 running → `store.put` 被呼叫一次帶 `meta.aiActive = true`；terminal → `meta.aiActive = false`；落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」與 scenarios「Multi-tab badge visibility」「Same-user multi-tab visibility」`store.put` 寫入行為。
- [x] 11.2 在 `apps/web/src/agent/cursor-ai-badge.ts` rewrite：`AiBadgeEditor` interface 從 `updateInstanceState({meta})` 改成 `store.get(presenceId) + store.put + user.getId()`；`useCursorAiBadge` useEffect 呼叫 `InstancePresenceRecordType.createId(userId)` 拿 presence id → `editor.store.get(presenceId)` → 若 record 存在則 `put({...existing, meta: {...existing.meta, aiActive}})`；使 11.1 GREEN；落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」實作面。
- [x] 11.3 [P] 在 `apps/web/src/canvas/use-sync-store.test.ts` 加 RED 測試：local instancePresence.meta 寫入後，遠端 `getPresenceRecords()` 能讀到對應 `aiActive` 欄位；disconnect 後 presence record 自然清除；落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」與 scenarios「Multi-tab badge visibility」「Disconnect clears badge implicitly」sync round-trip。
- [x] 11.4 [P] 在 `apps/web/src/canvas/CollaboratorAvatars.tsx` 改讀邏輯：從 `presence.meta?.aiActive` 取（取代既有 `userMeta.aiActive`）；以 CollaboratorAvatars.test.tsx 加 RED → GREEN：兩 collaborator presence meta.aiActive 一 true 一 false → 只 true 的 avatar 有 overlay；落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」UI 讀取面。
- [x] 11.5 在 `apps/web/src/canvas/Editor.tsx` 確保傳給 `AiSidePanel` 的 `editor` prop 暴露的 `AiBadgeEditor` interface 對齊新 narrow surface（含 `store.get` / `store.put` / `user.getId`）；既有 `editorInstance as unknown as AiBadgeEditor` cast 仍 work；以 Editor.test.tsx regression 驗。
- [x] 11.6 [P] 在 `e2e/agent-multi-tab-badge.spec.ts` 翻 `test.skip(true, "M15 — cross-tab broadcast pending (#16). Spec body kept ready.")` flag 移除；spec body 不動；驗 cross-tab + cross-user multi-tab visibility scenario 真實跑綠；落實 spec ai-side-panel MODIFIED Requirement「Cursor AI Badge surfaces aiActive presence flag」與 scenarios「Multi-tab badge visibility」「Same-user multi-tab visibility」「Disconnect clears badge implicitly」「Badge channel never carries thread content」端到端。

## 12. E2E mcp-server-roundtrip + 舊 .agents / .claude/skills/ gitignore（落實 spec mcp-server ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」與 design「Stateless Streamable HTTP, single endpoint POST /api/mcp」端到端 + workflow hygiene）

- [x] 12.1 在 `.gitignore` 加 `.claude/skills/` 與 `.agents/` 兩條（既有手動加過的話 verify 仍在）；以 `git status` 確認兩個 path 不再 untracked。
- [x] 12.2 [P] 在 `e2e/mcp-server-roundtrip.spec.ts` 寫 Playwright：sign-in 後 `page.request.post('/api/account/pat', {data: {name: "e2e", expiresInDays: 30}})` 拿 plaintext → 用 plaintext 對 `/api/mcp` 連送 4 個 request（initialize → ping → tools/list → tools/call createShape）→ 驗每個 response shape；最後 `page.goto('/canvas/<id>')` 看到 createShape 結果落在 canvas。覆蓋 spec mcp-server ADDED Requirement「Stateless Streamable HTTP endpoint accepts JSON-RPC 2.0 at POST /api/mcp」端到端 + scenarios「Valid JSON-RPC request returns matching JSON-RPC response」「initialize returns server info and tools capability」「ping returns empty result」「tools/list enumerates exactly twelve tools」「createShape with owner role dispatches mutator」。
- [x] 12.3 [P] 在 `e2e/mcp-server-roundtrip.spec.ts` 加 negative test 第二個 `test()`：用 expired PAT 對 `/api/mcp` → 期待 HTTP 401；落實 spec mcp-server ADDED Requirement「PAT authentication gate sits in front of JSON-RPC dispatch」與 scenario「Expired PAT returns HTTP 401」端到端。

## 13. Quality gates + manual smoke + 收尾

- [x] 13.1 [P] 執行 `bun run typecheck` 全綠；確認 apps/api/src/mcp + apps/api/src/pat + apps/api/src/sync/list-canvases-reader.ts + apps/web/src/account 四個目錄無 any、deps interface 顯式宣告。
- [x] 13.2 [P] 執行 `bunx oxlint apps/api/src/mcp apps/api/src/pat apps/api/src/sync/list-canvases-reader.ts apps/web/src/account` + `bunx oxfmt --check` 全綠。
- [x] 13.3 執行 `bun test --coverage`；新加 mcp / pat / list-canvases / web account 四個目錄 ≥ 70% lines / branches；補測 gap 至達標。
- [x] 13.4 執行 `bun run test:e2e`；本 milestone 新 mcp-server-roundtrip 兩個 case 綠；翻 skip 後 agent-multi-tab-badge 綠；既有 happy-path e2e 不退步。
- [x] 13.5 Owner manual smoke：在 Claude Desktop 配置 `~/.config/claude/mcp.json` 帶剛建的 vellum PAT → 啟動 Claude Desktop → 對話「list my vellum canvases」確認 listCanvases 回正確 list → 對話「在 canvas <id> 加 3 個 markdown shape 內容 1/2/3」確認 createShape × 3 跑通 + 回 vellum 網頁該 canvas 看到 3 個 shape。
- [x] 13.6 在 `docs/PHASE2_MILESTONES.md` 把 M15 標記 ✅ v0.6.0（透過 review markdown 內容確認 status 欄已更新、版號對應仍正確）。
